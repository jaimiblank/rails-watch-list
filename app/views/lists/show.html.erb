<div class="container">
  <h2><%= @list.name.capitalize %></h2>
  <% @bookmarks.each do |bookmark| %>
  <%= bookmark.movie.title %>
  <%= bookmark.comment %>
  <% end %>

<%= link_to "Add a new bookmark to this list", new_list_bookmark_path(@list) %>
<%= link_to "🏠", lists_path %>
</div>
